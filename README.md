# testing-scripts
Shell scripts for reproducibly testing parts of the rover through ROS

The easiest way to run these scripts on the rover is to enter the `rover-ros2` directory before running them with `~/testing-scripts/<your-script>`

The full_headless script needs to be run in a kitty window with remote-control enabled. Run `kitty -o allow_remote_control=yes` to open one  
